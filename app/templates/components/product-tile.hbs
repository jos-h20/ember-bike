<div class="productTile">
  <h4>{{product.title}}</h4>
  {{#link-to 'product' product.id}}<img src="{{product.image}}">{{/link-to}}
  <p>{{product.description}}</p>
  ${{product.cost}}
  <p><button {{action 'addToCart' product}}>Add to Cart</button></p>
</div>
