import { addedToCart } from 'bike-shop/helpers/added-to-cart';
import { module, test } from 'qunit';

module('Unit | Helper | added to cart');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addedToCart([42]);
  assert.ok(result);
});
